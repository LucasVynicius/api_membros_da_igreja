spring:
  application:
    name: Gerenciamento de membros
  datasource:
    url: jdbc:postgresql://localhost:5432/gerenciador_de_membros
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: 1995
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
